# SmartFit-React-PowerdedByAi

Starter React + TypeScript + Vite app for SmartFit — AI-Powered.

This repository contains a minimal, production-oriented starter with: CI (GitHub Actions), TypeScript, Vite, Tailwind CSS, ESLint, Prettier, a basic AI client pattern, Dockerfile, and a recommended folder architecture.

Quick start:
1. npm ci
2. npm run dev

Environment:
- Create a .env.local with VITE_SMARTFIT_AI_KEY=your_api_key
